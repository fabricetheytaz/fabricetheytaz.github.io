
/*
 * @param {Number} cx
 * @param {Number} cy
 * @param {Number} r
 * @param {String} color
 * 
 * @returns {Function}
 *
function toCircle(cx, cy, r, color = "red")
	{
	return position =>
		{
		let circle = document.createElementNS("http://www.w3.org/2000/svg", "circle");

		circle.setAttribute("cx", cx + position.x);
		circle.setAttribute("cy", cy + position.y);
		circle.setAttribute("r", r);
		circle.setAttribute("fill", color);

		return circle;
		};
	}
*/
